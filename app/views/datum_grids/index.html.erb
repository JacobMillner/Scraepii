<%= stylesheet_link_tag "datagrid" %>
<%= datagrid_form_for @grid, :method => :get, :url => datum_grids_path %>

<%= link_to "Get CSV", url_for(:datum_grids_grid => @params, format: 'csv') %>
<p>
<%= will_paginate(@grid.assets) %>
<%= datagrid_table @grid %>
<%= will_paginate(@grid.assets) %>
